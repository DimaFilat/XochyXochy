const userReducer = (state = 0, action) => {
    switch (action.type) {
      case "ACTION":
        return;
      case "ACTION2":
        return;
      default:
        return state;
    }
  };
  
  export default userReducer;